[build-system]
requires = ["maturin>=1.1,<2.0"]
build-backend = "maturin"

[project]
name = "vorpal-cli"
version = "0.8.0"
description = "Structural Search and Rewrite code at large scale using precise AST pattern."
authors = [{ name = "Ada Lundhe", email = "adalundhe@lundhe.audio" }]
maintainers = [{ name = "Ada Lundhe", email = "adalundhe@lundhe.audio" }]
readme = "README.md"
license = { file = "LICENSE" }
keywords = [
  "ast",
  "pattern",
  "codemod",
  "structural search",
  "rewrite"
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Rust",
  "Topic :: Security",
  "Topic :: Software Development :: Quality Assurance",
  "Topic :: Software Development",
  "Topic :: Text Processing"
]


[project.urls]
Repository = "https://github.com/hyper-light/vorpal"
Documentation = "https://vorpal.github.io/"
Changelog = "https://github.com/hyper-light/vorpal/blob/main/CHANGELOG.md"


[tool.maturin]
bindings = "bin"
manifest-path = "crates/cli/Cargo.toml"
strip = true
