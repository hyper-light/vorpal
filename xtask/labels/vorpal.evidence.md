# Evidence for `xtask/labels/vorpal.json`

Corpus: this repository (the self-index), graded against v0.7.0 (`eaa0f7e`). Paths are
relative to the repo root; `file:line` is the definition, the quote is verbatim from the
doc comment / module doc / body that proves the grade-3 symbol is what the query means.
Grading is from the source only — never from what search returns. Paraphrase queries
share no token with the identifier (or its file name); descriptive queries share at most
one. `path` is recorded wherever a name is defined in more than one file (the harness
matches it with `ends_with`).

## original set (graded 2026-08-30)

- **exact** "rrf_fuse_explained" → `rrf_fuse_explained` (grade 3): crates/index/src/lib.rs:4045 — doc at :4043 "[`rrf_fuse`] with provenance: each fused hit carries its per-channel rank (0-based; `None` where a channel did not surface it)".
- **exact** "commit_generation" → `commit_generation` (grade 3): crates/index/src/lib.rs:1878 — `pub(crate) fn commit_generation(root: &Path, prior: &Path, staging: PathBuf) -> io::Result<String>` (no doc comment; the atomic `CURRENT` swap of a staged generation — see the generation layout in the module docs).
- **short-keyword** "warm ann" → `warm_ann` (grade 3): crates/index/src/lib.rs:2738 — doc at :2733 "Build the ANN tier now if it is stale — the daemon calls this eagerly (in the background, right after an index refresh)". [grade 2 `ensure_ann` — the query-side "build iff the stamp no longer matches" form, doc at :2730]
- **short-keyword** "resolve index dir" → `resolve_index_dir` (grade 3): crates/kg/src/kg.rs:326 — doc at :323 "Idempotent: a resolved generation directory contains no `CURRENT`, so re-resolving is a no-op — callers may resolve defensively at any boundary."
- **subset** "postings" → `Postings` (grade 3): crates/index/src/postings.rs:142 — doc at :140 "A loaded posting index. Small wrapper over the decoded token table; the ids pool stays one flat buffer."
- **descriptive** "bounded bfs edge budget" → `bounded_bfs` (grade 3): crates/query/src/exec.rs:1132 — doc at :1129 "Per-seed BFS bounded by depth and the shared edge budget; nodes first reached at depths in `[min, max]` are appended".
- **descriptive** "ingest folded stacks" → `ingest_traces` (grade 3): crates/index/src/traces.rs:103 — doc at :101 "Ingest one folded-stacks file into `index_dir`'s CURRENT generation. Returns the report; the sidecar lands only when at least one row resolved."
- **paraphrase** "near duplicate code detection" → `similar_pairs` (grade 3): crates/ingest/src/similar.rs:56 — doc at :55 "Similar pairs `(a, b, confidence)` with `a < b`, sorted; confidence = similarity × 100." [grade 1 `Sketch` — the token sketch the pairs are computed from]
- **paraphrase** "who called what at runtime" → `ObservedStore` (grade 2): crates/kg/src/observed.rs:63 — doc at :61 "The observed-call table for one generation: rows sorted by `(from, to)` plus a callee-sorted permutation for the inbound direction." [`ingest_traces` grade 2 — the ingest that fills it]
- **conjunctive** "louvain community size cap" → `cut` (grade 3): crates/kg/src/communities.rs:137 — doc at :134 "The reported community of each participant: the coarsest dendrogram ancestor with at most `cap` members (level 0 is the floor; cap 0 = the top level)."


## exact (added 2026-09-03)

- **exact** "calibrate_semantic_cutover" → `calibrate_semantic_cutover` (grade 3): crates/index/src/lib.rs:3815 — doc at :3801 "Measure, on THIS machine over THIS index's rows, the fetch width where the beam stops being faster than the flat exact scan — the mid-range routing crossover, LEARNED from ingested data at warm time."
- **exact** "coherent_persisted_embedder" → `coherent_persisted_embedder` (grade 3): crates/index/src/lib.rs:2418 — doc at :2413 "Query-side coherence: are the persisted vector artifacts internally consistent for `current_stamp` — stamp file, bin header, tier record, and (learned) the model file verified against the recorded checksum?"
- **exact** "generation_content_id_full" → `generation_content_id_full` (grade 3): crates/index/src/lib.rs:1433 — doc at :1429 "The pre-Merkle full-rehash fold over every artifact byte — the agreement oracle's reference (`tests/pack_v2.rs`): two generations agree under the Merkle id exactly when they agree under this." [grade 1 `generation_content_id` :1378 — the production Merkle fold this is the oracle for]
- **exact** "compile_match_regex" → `compile_match_regex` (grade 3): crates/query/src/expr.rs:706 — doc at :705 "Compile a `=~` pattern under the DoS bounds."
- **exact** "verify_and_extract" → `verify_and_extract` (grade 3): crates/loader/src/lib.rs:141 — doc at :137 "Read a `[header][payload]` stream from `reader`, verify blake3 **and** the Ed25519 signature against `verifying_key`, and return the recovered agent bytes." [grade 1 `sign_payload` :116 — the coordinator-side counterpart]
- **exact** "scc_sizes" → `scc_sizes` (grade 3): crates/kg/src/scc.rs:17 — doc at :15 "Component size per node over the CALLS edges in `edges`. `n` bounds the id space; out-of-range endpoints (impossible in a sealed writer, tolerated totally) are skipped."

## short-keyword (added 2026-09-03)

- **short-keyword** "bm25 inverse document frequency" → `bm25_idf` (grade 3): crates/index/src/postings.rs:46 — doc at :44 "Lucene's nonnegative IDF: ln(1 + (N − df + 0.5)/(df + 0.5)) — never negative, so a common token can dilute but not penalize (the short-query supremacy invariant)."
- **short-keyword** "bm25 term frequency saturation" → `bm25_term` (grade 3): crates/index/src/postings.rs:51 — doc at :50 "The BM25 term component: tf·(k1+1) / (tf + k1·(1 − b + b·dl/avgdl))."
- **short-keyword** "louvain local move" → `local_moving` (grade 3): crates/kg/src/communities.rs:300 — doc at :297 "One Louvain level: sweep nodes in id order, moving each to the neighbor community with the best modularity gain (ties: stay if staying ties, else the lowest community id), until a sweep moves nothing." [grade 1 `gain` :293 "Modularity gain of placing a node of degree `k_u` into community `c`" — the per-move scorer]
- **short-keyword** "greedy beam search" → `greedy_search` (grade 3): crates/ann/src/vamana.rs:151 — module doc at :1 "Vamana graph construction + greedy beam search (§10.2's DiskANN family, in-memory form)."; the fn takes `graph: &Adjacency<'_>` and is the module's search entry (no `///` of its own).
- **short-keyword** "ed25519 sign payload" → `sign_payload` (grade 3): crates/loader/src/lib.rs:116 — doc at :113 "Build the `[header][payload]` stream for `agent_bytes`, signed by `signing_key`. ... the signature is over the raw agent's blake3, so the loader can verify authenticity from a 32-byte message regardless of payload size." [grade 1 `sign_bytes_hex` :186 "Detached ed25519 signature over arbitrary bytes, hex-encoded — the release checksums signer (D6)."]
- **short-keyword** "wordpiece tokenizer from json" → `from_tokenizer_json` (grade 3): crates/ann/src/encoder/tokenizer.rs:34 — doc at :32 "Parse and VALIDATE a `tokenizer.json`: the model, normalizer, pre-tokenizer, and template must be exactly the pipeline this implementation reproduces." [grade 2 `WordPiece` :18 — the struct it constructs]
- **short-keyword** "canary extraction verify" → `verify_extraction` (grade 3): crates/ingest/src/selfcheck.rs:399 — doc at :394 "Run every canary through `extractor` and report all shortfalls at once." [grade 2 `verify_default_extraction` :510 "[`verify_extraction`] for the default (built-in rules) extractor, cached process-wide."; grade 1 `CANARIES` :45 "The full canary table"]
- **short-keyword** "compute dirty region" → `compute_dirty` (grade 3): crates/ingest/src/walk_reuse.rs:527 — doc at :522 "Map the incremental parse's delta to the dirty top-level region: the spanning textual edit, unioned with every tree-sitter changed range (mapped to old coordinates)" [grade 1 `DirtyRegion` :515 — the returned type]
- **short-keyword** "nearest index root ancestor" → `find_index_root` (grade 3): crates/index/src/lib.rs:3680 — doc at :3678 "The nearest ancestor directory holding an existing default-location index — a generation root (carrying `CURRENT`) or a legacy flat dir (carrying `manifest.bin` directly)."
- **short-keyword** "checksum pinned download" → `fetch_pinned` (grade 3): crates/index/src/models.rs:177 — doc at :174 "Download `file` into `dir` with streaming sha256 verification: `.part` + atomic rename; a checksum mismatch deletes the download and errors."

## subset (added 2026-09-03)

- **subset** "parse health" → `parse_health_report` (grade 3): crates/index/src/lib.rs:6276 — doc at :6271 "The parse-health query (IMPROVEMENTS #11): per-file damage with everything a consumer needs before treating missing relations as meaningful absence"
- **subset** "include reach" → `IncludeReach` (grade 3): crates/resolve/src/reach.rs:25 — doc at :23 "Per-file transitive include reachability. Immutable after construction; safe to share across resolution shards (`&self` queries only)."
- **subset** "dataflow" → `DataflowStore` (grade 3): crates/kg/src/dataflow.rs:166 — doc at :165 "Read-side handle. Absent-tolerant: a generation without the sidecar answers empty." (module doc :1 "The data-flow sidecar (`dataflow.bin`, VDFL v1 — G-M3)") [grade 2 `load_dataflow` :56 "Load every row of `dir/dataflow.bin`"; grade 1 `DataflowRow` :22 "One traceable argument at a resolved call site."]
- **subset** "bm25 override" → `set_bm25_override` (grade 3): crates/index/src/lib.rs:2820 — doc at :2815 "Manually override the per-corpus BM25 verdict (`vorpal tune`'s write path): rewrite the committed tier record with the given verdict and a stated manual-evidence line"
- **subset** "content id" → `generation_content_id` (grade 3): crates/index/src/lib.rs:1378 — doc at :1373 "The content id of a generation directory: the deterministic fold over every artifact (in fixed order) that names `gen/<id>` dirs." [grade 2 `generation_content_id_full` :1433 — the full-rehash oracle form]
- **subset** "gen diff" → `GenDiff` (grade 3): crates/index/src/gendiff.rs:82 — doc at :81 "The whole diff, node changes kept as cheap ids for page-materialization by the caller." [grade 2 `diff` (path crates/index/src/gendiff.rs) :115 "Diff two open generations. Deterministic: files in path order, node changes in (file, eid) order." — `diff` is also defined twice in crates/cli/src/print/json_print.rs, hence the path]

## descriptive (added 2026-09-03)

- **descriptive** "process unique staging suffix" → `staging_nonce` (grade 3): crates/index/src/lib.rs:1873 — doc at :1871 "Process-unique staging suffix: concurrent builds in ONE process (the daemon's background canonicalizer racing a synchronous rebuild) must never share a staging directory."
- **descriptive** "per row int8 codes with scale" → `quantize_row` (path crates/ann/src/dense.rs) (grade 3): crates/ann/src/dense.rs:27 — doc at :25 "Symmetric int8 quantization of one row: `scale = max|x| / 127`, codes round-to-nearest. A zero row keeps scale 0 and all-zero codes." [grade 2 `quantize_row` (path crates/ann/src/qmatrix.rs) :127 — module doc :1 "Per-row i8 quantization of the vector matrix, with exact integer dot products."; the name is defined in three files (dense.rs, qmatrix.rs, encoder/gemm_i8.rs), hence paths]
- **descriptive** "alpha dominated neighbor pruning" → `robust_prune` (grade 3): crates/ann/src/vamana.rs:585 — doc at :574 "DiskANN's robust prune: repeatedly keep the closest candidate and discard candidates it α-dominates, bounding degree at `r` while preserving reach in all directions."
- **descriptive** "first paragraph of definition body" → `body_head` (grade 3): crates/index/src/dense.rs:278 — doc at :276 "The definition span's first paragraph: its lines up to (not including) the first blank line, or the span end."
- **descriptive** "comment block above definition" → `leading_comment` (grade 3): crates/index/src/dense.rs:236 — doc at :233 "The contiguous comment block immediately above the line containing `span_start`, markers stripped, whitespace collapsed."
- **descriptive** "can this file event change the index" → `event_is_relevant` (grade 3): crates/mcp/src/watch.rs:153 — doc at :152 "Whether an event can possibly affect index contents." [grade 2 `path_is_relevant` :173 "Whether a changed path can possibly affect index contents: not inside a hidden tree ... and not gitignored"]
- **descriptive** "build symbol table from graph" → `from_kg` (grade 3): crates/resolve/src/table.rs:352 — doc at :348 "Build a table from every node in a sealed [`Kg`]. `File` nodes go to the path map (targets of path-form imports); import/alias nodes are wiring, not definitions, and are never candidates" [grade 1 `SymbolTable` :67 — the type built]
- **descriptive** "total order for evidence rows" → `sort_canonical` (grade 3): crates/kg/src/evidence.rs:228 — doc at :226 "Canonically sort rows: the total order that makes the artifact a pure function of the occurrence set — deterministic under any sort algorithm and any thread count."
- **descriptive** "files that mention a name hash" → `files_referencing` (grade 3): crates/kg/src/usagestore.rs:442 — doc at :439 "Every file (by key) whose references mention the name with this hash, ascending."
- **descriptive** "remove the global encoder enable" → `disable_global` (grade 3): crates/index/src/models.rs:80 — doc at :78 "Remove the global enable (per-index selections are untouched). `Ok(false)` when nothing was enabled." [grade 2 `disable_variant` :313 "Variant-checked disable: remove the global enable IFF it points at this variant's install directory."]
- **descriptive** "spawn detached background warm" → `maybe_spawn` (grade 3): crates/index/src/autowarm.rs:81 — doc at :78 "Spawn a detached warm of `index_dir`, if this process may: registered, not vetoed, not already spawned this process." [grade 1 `run_if_sentinel` :37 "If this process's argv is a warm-sentinel invocation, run the warm and exit the process." — the child side]

## paraphrase (added 2026-09-03)

- **paraphrase** "definition text presented to encoder" → `SurfaceRecipe` (grade 3): crates/index/src/dense.rs:75 — doc at :67 "What text a definition presents to the encoder — ONE recipe for the sidecar build and the query-time rerank (the one-recipe law" [grade 2 `SurfaceBuilder` :302 "Builds candidate surfaces under the active recipe — shared by the sidecar build and the rerank."]
- **paraphrase** "which definitions the fill embeds" → `CoverageRule` (grade 3): crates/index/src/dense.rs:134 — doc at :120 "Which definitions the always-on fill embeds (the stop rule). Both are structural — no size constant — and both were measured"
- **paraphrase** "run indexer as child process" → `Supervisor` (grade 3): crates/mcp/src/supervised.rs:40 — module doc at :1 "Supervised index builds (D3): the daemon runs the indexer as a **child process**, so one pathological file — a scanner segfault, a runaway allocation, an OOM kill — costs one build attempt and an error string, never the server." [grade 1 `BuildOutcome` :32 "The outcome of asking for a supervised build."]
- **paraphrase** "why no edge to a name" → `explain_absence_on` (grade 3): crates/index/src/lib.rs:6360 — doc at :6357 "Answer \"why is there NO edge from this node to anything named `name`?\" — the no-edge occurrences (external/masked) the resolver retained for that referenced name"
- **paraphrase** "skip entity whose content hash matches" → `is_unchanged` (grade 3): crates/canonical/src/index.rs:126 — doc at :124 "The incremental-skip check (§3.4): the key exists and its content is unchanged, so ingest can skip read/parse/extract/embed for it."
- **paraphrase** "files edited together in git history" → `Cochange` (grade 3): crates/index/src/cochange.rs:43 — doc at :42 "The pass outcome: edges (symmetric pairs listed once, `a < b`) and a human note when nothing could be computed."; module doc :1 "Git co-change (temporal coupling) edges — ADOPTION #27's second half. Files that keep changing together in recent history are coupled whether or not any static edge says so" [grade 2 `CochangeEdge` :36 "One co-change edge: manifest paths of both files plus the packed confidence."; grade 1 `count_pairs` :276 "Count co-changed pairs over git's own paths"] [path: `crates/index/src/cochange.rs` — the name is defined in more than one file]
- **paraphrase** "serialize a reference to fixed width bytes" → `encode_record` (grade 3): crates/resolve/src/spill.rs:70 — `pub(crate) fn encode_record(reference: &Reference, buf: &mut [u8; RECORD])`; the record contract is the module doc :10 "Records are fixed-width 34-byte little-endian, portable by construction" and `RECORD` doc :24 "Bytes per spilled reference record." [grade 2 `decode_record` :88 — the inverse]
- **paraphrase** "greedy longest match segmentation" → `word_pieces` (grade 3): crates/ann/src/encoder/tokenizer.rs:182 — doc at :180 "Greedy longest-match WordPiece: first piece bare, continuations prefixed; an unmatchable word (or one over the char cap) is one `[UNK]`."

## conjunctive (added 2026-09-03)

- **conjunctive** "gpu weights resident once" → `make_resident` (grade 3): crates/ann/src/encoder/gemm_wgpu.rs:515 — doc at :513 "Upload one weight matrix (row-major `[rows_out][dim_in]`, as the caller will later pass it by reference) so GEMMs against it read device memory."; module doc :8 "Weights become RESIDENT once per model open ([`GpuGemm::make_resident`] — every layer matrix, ≈453 MB for CodeRankEmbed)". Both concepts (GPU path, weights resident once) are in the module doc. [grade 2 `GpuGemm` :263 "A compiled GEMM pipeline on one adapter with the model's weights resident."]
- **conjunctive** "bm25 ranking without posting index" → `bm25_exhaustive` (grade 3): crates/index/src/postings.rs:312 — doc at :306 "Channel parity when the posting tier is missing or stale: the IDENTICAL BM25 ranking computed in one pass over the graph — same df/avgdl/N derivation". Both concepts (BM25 ranking; no posting tier) hold. [grade 2 `bm25_ranked` :258 "The BM25 channel: rank every ADMITTED node matching ANY query token" — the posting-tier form]
- **conjunctive** "f16 model variant install" → `install` (path crates/index/src/models.rs) (grade 3): crates/index/src/models.rs:239 — doc at :235 "Install the requested variant under the models root (or `root` when given) and return the installed model directory. f32 = the pinned upstream bytes; f16 = pinned f32 downloaded ... converted locally". Both concepts (f16 variant; install). `install` is also defined in crates/pyo3/src/async_bridge.rs, hence the path. [grade 2 `ModelVariant` :132 "Which weight precision to install."]
- **conjunctive** "retrofit edges from graph relations" → `build_retro_edges` (grade 3): crates/index/src/lib.rs:3162 — doc at :3141 "Assemble the retrofit adjacency over the semantic ROW space from the graph's typed, confidence-graded edges — everything edge-specific folds into the weights here, so the solver (`vorpal_ann::retrofit`) stays pure algebra with no graph dependency." Both concepts (retrofit; edges derived from graph relations) hold. [grade 1 `retrofit_into` crates/ann/src/retrofit.rs:180 — the graph-free solver these edges feed]
